# Android app

Assembleable Gradle project. Application id `com.clintmaples.broadcastifyscanner`, **versionName 0.2.0**, **versionCode 2**.

```bash
./gradlew assembleRelease
./gradlew test
```

On Windows: `gradlew.bat assembleRelease`.

See the repo root README for APK install, HLS token scrape, and UX notes.
